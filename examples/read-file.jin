import std.(
    fs,
    fs.OpenOptions
)

fn main() = {
    let file = std.fs.open(
        "/home/ron/file.txtt", 
        OpenOptions.new().read(true)
    )

    match file {
        Result.Ok(file) -> {}
        Result.Err(err) -> println("Error: {err}")
    }
}
