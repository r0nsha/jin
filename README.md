# Jin

Jin is a statically typed, procedural and safe programming language.

## Hello, World

```
fn main() {
    println("Hello, World!")
}
```

## Highlights

- Automatic, deterministic memory management inspired by [Vale](https://vale.dev/)'s generational references
- Pattern matching and tagged union types
- Parametric polymorphism
- Local type inference with required top level type annotations
- Compiled to machine code (Currently compile to C)

## Goals

- Simple enough that it can be learned in an afternoon
- Fast compile times
- Performance that statisfies use-cases such as servers and CLI applications

## Prerequisites

TODO

## Usage

Run using `jin run <file>` or build & run using `jin build <file> && ./<file>`.

## Building from source

TODO
