type Box(value: int)

fn main() unit = {
	let box = Box(1)
	//box.copy()

	//let ref = &box
	//let jeff = ref.copy()
	//ref.copy().copy()

	{ &box }.copy().copy()
}

//fn copy(b: Box) &Box = &b
fn copy(b: &Box) &Box = b
