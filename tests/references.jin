type Box(value: int)

fn main() unit = {
	let box = Box(1)
	//box.copy()

	let ref = &box
	ref.copy()
	let ref2 = ref.copy()
	ref.copy().copy()
	let ref3 = ref.copy().copy()
	{ &box }.copy().copy()
}

//fn copy(b: Box) &Box = &b
//fn copy(b: &Box) &Box = b

fn copy[T](b: T) T = b
//fn copy[T](b: T) &T = b
