fn extern printf(fmt: *u8, ..) i32

type Option[a] {
	some(value: a),
	none
}

fn main() unit = {
	//let x = Option.some
	let x = Option.some(1)
	//let x = Option.some(value: 2)
	//let y = Option.none
}
