fn extern printf(fmt: *u8, ..) i32

type Option[a] {
	some(value: a),
	none
}

fn main() unit = {
	let a = Option.some(1)
	let b = Option[int].some(1)
	let c = Option.some(value: 2)
	let d = Option[int].none
}
