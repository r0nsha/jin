fn extern printf(fmt: *u8, ..) i32

type Option {
	some(value: int),
	none()
}

fn main() unit = {
	let x = Option.some
	//let x = Option.some(1)
	//let x = Option.some(value: 1)
	//let x = Option.some(x: 1)
	//let x = Option.some(1, 2)
	//let x = Option.some()
	//let y = Option.none()
}
