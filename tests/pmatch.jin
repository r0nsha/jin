fn extern printf(fmt: *u8, ..) i32

fn main() unit = {
	//match true {
	//	mut x -> {
	//		x = false
	//		printf("x = %d\n".ptr, x)
	//	},
	//	_ -> {
	//		// ignored...
	//	},
	//}

	match_unit()
	match_bool(true)
	match_bool(false)

}

fn match_unit() unit = {
	match {} {
		{} -> printf("matched unit!\n".ptr),
		_ -> 1, // This branch is unreachable
	}
}

fn match_bool(value: bool) unit = {
	printf("value = %s\n".ptr, match value {
		true -> "true",
		false -> "false",
		x -> "unreachable..."
	})
}
