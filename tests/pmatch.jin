fn extern printf(fmt: *u8, ..) i32

fn main() unit = {
	let foo = true

	match foo {
		bar -> { printf("bar=%d\n".ptr, bar) },
		//_ -> { printf("bar=%d\n".ptr, bar) },
	}

	//let foo_str = match foo {
	//	true -> "true",
	//	false -> "false"
	//}
	//printf("foo = %s\n".ptr, foo_str)
}
