fn extern printf(fmt: *u8, ..) i32

fn main() unit = {
	//match true {
	//	mut x -> {
	//		x = false
	//		printf("x = %d\n".ptr, x)
	//	},
	//	_ -> {
	//		// ignored...
	//	},
	//}

	printf("foo = %s\n".ptr, true.to_str().ptr)

	let _ = match {} {
		{} -> printf("matched unit!\n".ptr),
		_ -> 1, // This branch is unreachable
	}
}

fn to_str(value: bool) str =
	match value {
		true -> "true",
		false -> "false",
		_ -> "unreachable..."
	}
