fn extern printf(fmt: *u8, ..) i32

fn main() unit = {
	let foo = true

	match foo {
		foo -> { printf("foo is %d\n".ptr, foo) }
	}

	//let foo_str = match foo {
	//	true -> "true",
	//	false -> "false"
	//}
	//printf("foo = %s\n".ptr, foo_str)
}
