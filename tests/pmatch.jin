fn extern printf(fmt: *u8, ..) i32

fn main() unit = {
	match true {
		foo -> { printf("foo=%d\n".ptr, foo) },
		_ -> {
			// ignored...
		}
	}

	let foo = true

	//let foo_str = match foo {
	//	true -> "true",
	//	false -> "false"
	//}
	//printf("foo = %s\n".ptr, foo_str)
}
