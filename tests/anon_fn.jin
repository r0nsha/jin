import std.option.{Option}
import std.io.{println}

fn extern "c" printf(fmt: *u8, ..) -> i32

fn main() = {
	if Option.some(1).map_or(false) fn(x) { x == 1 } {
		call(fn { println("hello") })
	}
}

fn call(f: fn()) = f()
