fn extern "c" printf(fmt: *u8, ..) -> i32

fn main() = {
	let a = 2.0
	let b = 6.0

	unsafe printf("-a = %f\n".data, -a)
	unsafe printf("a + b = %f\n".data, a + b)
	unsafe printf("a - b = %f\n".data, a - b)
	unsafe printf("a * b = %f\n".data, a * b)
	unsafe printf("a / b = %f\n".data, a / b)
}
