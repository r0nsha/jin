fn extern printf(fmt: *u8, ..) i32

type Wrapper(num: Num, other:int)
type Num(inner: int)

fn main() unit = {
	let x = 1
	let y = x
	let z = x
	let z2 = x
	//Num(1).print()
}

fn print(num: Num) unit = {
	//printf("Num(%d)\n".ptr, num.inner)
}
