fn main() = {
	add(41, true) // valid
	// add(41, b = 1) // valid
	// add(a = 41, 1) // valid
	// add(b = 41, 1) // valid
	// add(a = 41, b = 1) // valid
	// add(b = 41, a = 1, b = 5) // invalid (named passed twice)
	// add(a = 41, a = 1, b = 5) // invalid (named passed twice)
	// add(41, a = 1) // invalid (named argument already passed as positional)
	return ()
}

fn add(a, b) = a + b
