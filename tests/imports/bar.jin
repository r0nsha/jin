use imports as root
pub use .baz

fn extern "c" printf(fmt: *U8, ..) -> I32

pub fn hello() = {
	unsafe printf(root.get_hello_world().ptr)
}

pub fn goodbye() = {
	unsafe printf("Goodbye...\n".ptr)
}
