use .bar.baz.yup
use .bar.(*)
use .ufcs.(?)

fn main() = {
	hello()
	yup()
	goodbye()

	# Ufcs imports
	1.print_int()
	#print_int(1) # Error!
}

pub fn get_hello_world() -> &str = "Hello from bar!\n"
