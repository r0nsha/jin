fn extern "c" printf(fmt: ptr[U8], ..) -> I32

fn main() = {
    let box = Box.default()
    box.print()
}

type Box(value: Int)

fn Box.default() -> Box = Box(0)

fn print(self: &Box) = {
    unsafe printf("Box(%d)\n".data, self.value)
}
